# JitPack configuration
jdk:
  - openjdk17
before_install:
  - sdk install maven 3.9.9
  - sdk use maven 3.9.9
  - sed -i '/<module>agentscope-examples<\/module>/d' pom.xml
install:
  - mvn clean install -DskipTests -Dspotless.check.skip=true -Drevision=$VERSION
